# ZenCube GUI Requirements
# Python 3.7+ required

# Modern GUI Framework (PySide6)
PySide6>=6.5.0
psutil>=5.9.0
matplotlib>=3.7.0
prometheus-client>=0.17.0

# Legacy GUI (Optional - for backwards compatibility)
# tkinter is included with most Python installations
# If tkinter is not installed, install it separately:
#   - Windows: Included with official Python installer
#   - Ubuntu/Debian: sudo apt-get install python3-tk
#   - macOS: Included with system Python or Homebrew Python

# All other modules are part of Python standard library:
# - subprocess (process execution)
# - threading (async operations)
# - os, sys, pathlib (file operations)
# - platform (OS detection)

# Optional: Development tools (uncomment if needed)
# pylint>=2.15.0
# black>=22.0.0
# pytest>=7.0.0
